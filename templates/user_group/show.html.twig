{% extends 'base.html.twig' %}

{% block title %}UserGroup{% endblock %}
{% block pageheader %}
    <a href="{{ path('user_group_index') }}" ><span class="fa fa-arrow-left fa-2x"></span> </a><h1 class="inline">UserGroup</h1>

{% endblock %}
{% block body %}


    <table class="table">
        <tbody>
            <tr>
                <th>#</th>
                <td>{{ user_group.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ user_group.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ user_group.description }}</td>
            </tr>
            <tr>
                <th>Created at</th>
                <td>{{ user_group.createdAt ? user_group.createdAt|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Updated at</th>
                <td>{{ user_group.updatedAt ? user_group.updatedAt|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>


    <a class="btn btn-success" href="{{ path('user_group_edit', {'id': user_group.id}) }}"><span class="fa fa-edit "></span> edit</a>

    {{ include('user_group/_delete_form.html.twig') }}
{% endblock %}
