{% extends 'base.html.twig' %}

{% block title %}ResearcherResearchTeam{% endblock %}

{% block body %}
    <h1>ResearcherResearchTeam</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ researcher_research_team.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('researcher_research_team_index') }}">back to list</a>

    <a href="{{ path('researcher_research_team_edit', {'id': researcher_research_team.id}) }}">edit</a>

    {{ include('researcher_research_team/_delete_form.html.twig') }}
{% endblock %}
