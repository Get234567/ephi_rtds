{% extends 'base.html.twig' %}

{% block title %}StudyResearchTeam index{% endblock %}

{% block body %}
    <h1>StudyResearchTeam index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for study_research_team in study_research_teams %}
            <tr>
                <td>{{ study_research_team.id }}</td>
                <td>
                    <a href="{{ path('study_research_team_show', {'id': study_research_team.id}) }}">show</a>
                    <a href="{{ path('study_research_team_edit', {'id': study_research_team.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('study_research_team_new') }}">Create new</a>
{% endblock %}
