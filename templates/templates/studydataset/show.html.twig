{% extends 'base.html.twig' %}

{% block title %}Studydataset{% endblock %}

{% block body %}
    <h1>Studydataset</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ studydataset.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ studydataset.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('studydataset_index') }}">back to list</a>

    <a href="{{ path('studydataset_edit', {'id': studydataset.id}) }}">edit</a>

    {{ include('studydataset/_delete_form.html.twig') }}
{% endblock %}
