{% extends 'base.html.twig' %}
{% block title %}Affiliation{% endblock %}
{% block breadcrumb %}
  <li>
								<i class="ace-icon fa fa-home home-icon"></i>
								<a href="{{ path('home')}}">Home</a>
								<a href="{{ path('affiliation_index') }}">Affiliation</a>
							</li>
							
							<li class="active">Show</li>
{% endblock %}
{% block nopageheader %}
    <a class="btn btn-primary"  href="{{ path('affiliation_index') }}">back to list</a>
{% endblock %}

{% block body %}
    <h1>Affiliation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ affiliation.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ affiliation.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ affiliation.description }}</td>
            </tr>
        </tbody>
    </table>

    

    <a class="btn btn-success" href="{{ path('affiliation_edit', {'id': affiliation.id}) }}">edit</a>

    {{ include('affiliation/_delete_form.html.twig') }}
{% endblock %}
