{% extends 'base.html.twig' %}

{% block title %}StudyFundingSource index{% endblock %}

{% block body %}
    <h1>StudyFundingSource index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for study_funding_source in study_funding_sources %}
            <tr>
                <td>{{ study_funding_source.id }}</td>
                <td>{{ study_funding_source.description }}</td>
                <td>
                    <a href="{{ path('study_funding_source_show', {'id': study_funding_source.id}) }}">show</a>
                    <a href="{{ path('study_funding_source_edit', {'id': study_funding_source.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('study_funding_source_new') }}">Create new</a>
{% endblock %}
