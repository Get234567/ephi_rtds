{% extends 'base.html.twig' %}

{% block title %}StudyFundingSource{% endblock %}

{% block body %}
    <h1>StudyFundingSource</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ study_funding_source.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ study_funding_source.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('study_funding_source_index') }}">back to list</a>

    <a href="{{ path('study_funding_source_edit', {'id': study_funding_source.id}) }}">edit</a>

    {{ include('study_funding_source/_delete_form.html.twig') }}
{% endblock %}
