{% extends 'base.html.twig' %}

{% block title %}StudySubject{% endblock %}

{% block body %}
    <h1>StudySubject</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ study_subject.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ study_subject.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ study_subject.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('study_subject_index') }}">back to list</a>

    <a href="{{ path('study_subject_edit', {'id': study_subject.id}) }}">edit</a>

    {{ include('study_subject/_delete_form.html.twig') }}
{% endblock %}
