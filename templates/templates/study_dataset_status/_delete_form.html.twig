<form method="post" action="{{ path('study_dataset_status_delete', {'id': study_dataset_status.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ study_dataset_status.id) }}">
    <button class="btn">Delete</button>
</form>
