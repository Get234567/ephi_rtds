{% extends 'base.html.twig' %}

{% block title %}data owner{% endblock %}

{% block body %}
    <h1>Data owner</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ row.ownerid }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ row.parentid }}</td>
            </tr>

            <tr>
                <th>Data owner</th>
                <td>{{ row.unit }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('Data_unit_index') }}">back to list</a>

    <a href="{{ path('Data_unit_edit', {'id': row.ownerid}) }}">edit</a>

    {{ include('dataowner/_delete_form.html.twig') }}
{% endblock %}
