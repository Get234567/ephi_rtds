{% extends 'base.html.twig' %}

{% block title %}InvolvementType{% endblock %}

{% block body %}
    <h1>InvolvementType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ involvement_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ involvement_type.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ involvement_type.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('involvement_type_index') }}">back to list</a>

    <a href="{{ path('involvement_type_edit', {'id': involvement_type.id}) }}">edit</a>

    {{ include('involvement_type/_delete_form.html.twig') }}
{% endblock %}
