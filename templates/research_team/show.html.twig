{% extends 'base.html.twig' %}

{% block title %}ResearchTeam{% endblock %}

{% block body %}
    <h1>ResearchTeam</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ research_team.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ research_team.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ research_team.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('research_team_index') }}">back to list</a>

    <a href="{{ path('research_team_edit', {'id': research_team.id}) }}">edit</a>

    {{ include('research_team/_delete_form.html.twig') }}
{% endblock %}
