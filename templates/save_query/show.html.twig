{% extends 'base.html.twig' %}

{% block title %}SaveQuery{% endblock %}

{% block body %}
    <h1>SaveQuery</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ save_query.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ save_query.name }}</td>
            </tr>
            <tr>
                <th>Query</th>
                <td>{{ save_query.query }}</td>
            </tr>
            <tr>
                <th>SavedAt</th>
                <td>{{ save_query.savedAt ? save_query.savedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('save_query_index') }}">back to list</a>

    <a href="{{ path('save_query_edit', {'id': save_query.id}) }}">edit</a>

    {{ include('save_query/_delete_form.html.twig') }}
{% endblock %}
