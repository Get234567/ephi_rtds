{% extends 'base.html.twig' %}

{% block title %}StudyType index{% endblock %}

{% block body %}
    <h1>StudyType index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for study_type in study_types %}
            <tr>
                <td>{{ study_type.id }}</td>
                <td>{{ study_type.name }}</td>
                <td>{{ study_type.description }}</td>
                <td>
                    <a href="{{ path('study_type_show', {'id': study_type.id}) }}">show</a>
                    <a href="{{ path('study_type_edit', {'id': study_type.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('study_type_new') }}">Create new</a>
{% endblock %}
