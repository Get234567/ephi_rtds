{% extends 'base.html.twig' %}

{% block title %}StudyType{% endblock %}

{% block body %}
    <h1>StudyType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ study_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ study_type.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ study_type.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('study_type_index') }}">back to list</a>

    <a href="{{ path('study_type_edit', {'id': study_type.id}) }}">edit</a>

    {{ include('study_type/_delete_form.html.twig') }}
{% endblock %}
