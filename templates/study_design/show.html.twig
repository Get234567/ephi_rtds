{% extends 'base.html.twig' %}

{% block title %}StudyDesign{% endblock %}

{% block body %}
    <h1>StudyDesign</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ study_design.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ study_design.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ study_design.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('study_design_index') }}">back to list</a>

    <a href="{{ path('study_design_edit', {'id': study_design.id}) }}">edit</a>

    {{ include('study_design/_delete_form.html.twig') }}
{% endblock %}
