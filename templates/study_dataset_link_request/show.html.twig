{% extends 'base.html.twig' %}

{% block title %}StudyDatasetLinkRequest{% endblock %}

{% block body %}
    <h1>StudyDatasetLinkRequest</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ study_dataset_link_request.id }}</td>
            </tr>
            <tr>
                <th>RequestedAt</th>
                <td>{{ study_dataset_link_request.requestedAt ? study_dataset_link_request.requestedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ study_dataset_link_request.Status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('study_dataset_link_request_index') }}">back to list</a>

    <a href="{{ path('study_dataset_link_request_edit', {'id': study_dataset_link_request.id}) }}">edit</a>

    {{ include('study_dataset_link_request/_delete_form.html.twig') }}
{% endblock %}
